[package]
name = "delivery_tauri"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { workspace = true }

# Nossos módulos de negócio
library = { path = "../library" }
playback = { path = "../playback" }
intelligence = { path = "../intelligence" }

# Serialização para conversar com o Zod/React
serde = { workspace = true }
serde_json = { workspace = true }
